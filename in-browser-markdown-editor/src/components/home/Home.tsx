import React from "react"
import styled from "styled-components"

interface HomeProps {
  inputRef: React.RefObject<any>
  showSidebar: boolean
}

const Home: React.FC = () => {
  return (
    <div>Home</div>
  )
}

export default Home